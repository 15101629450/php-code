// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("adx", "for adx support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("adx", "enable adx support", "no");

if (PHP_ADX != "no") {
	EXTENSION("adx", "adx.c");
}

